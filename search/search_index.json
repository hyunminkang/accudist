{"config":{"lang":["en"],"separator":"[\\s\\-]+","pipeline":["stopWordFilter"],"fields":{"title":{"boost":1000.0},"text":{"boost":1.0},"tags":{"boost":1000000.0}}},"docs":[{"location":"","title":"accudist","text":"<p>Probability distributions with R-grade numerical precision, as NumPy ufuncs.</p> <p>accudist wraps R 4.5.2's <code>nmath</code> C library, the code behind R's <code>pnorm</code>, <code>qbeta</code>, <code>ppois</code> and friends, and exposes it in Python with R's names, argument order and defaults. Its reason to exist is the tails: R computes upper tails and log-probabilities directly, where <code>scipy.stats</code> computes <code>1 - cdf</code> and underflows.</p> <pre><code>import accudist as ad\n\nad.ppois(200, 0.1, lower_tail=False, log=True)   # -1331.4544006213939  (scipy.stats: -inf)\nad.qnorm(-1000, log=True)                          # -44.61574773...      (scipy.stats: no log_p)\n</code></pre> call R 4.5.2 accudist scipy 1.17 <code>ppois(200, 0.1, lower=F, log=T)</code> -1331.454401 -1331.454401 -inf <code>pbinom(900, 1000, 1/6, lower=F, log=T)</code> -1312.687973 -1312.687973 -inf <code>pgamma(1e5, 2, lower=F, log=T)</code> -99988.48706 -99988.48706 -inf <code>qbeta(-1000, 0.5, 0.5, log.p=T)</code> 1.11e-308 1.11e-308 0.0 <p>Two things to know before you <code>pip install</code></p> <p>Licence. accudist is GPL-2.0-or-later because it links R's GPL sources. Importing it makes the importing work subject to the GPL. See Licence.</p> <p>Random numbers. <code>r*</code> functions do not reproduce R's default <code>set.seed()</code> streams. They reproduce R only under <code>RNGkind(\"Marsaglia-Multicarry\")</code>. See Random numbers.</p>"},{"location":"#where-to-go","title":"Where to go","text":"<ul> <li>Installation: wheels, source builds, uv.</li> <li>Quickstart: five minutes with the API.</li> <li>R to accudist: naming rules, dispatch, argument-order hazards.</li> <li>Precision: what \"eight significant digits against R\" means and how it is tested.</li> <li>API reference: every function, its R equivalent, and notes.</li> </ul>"},{"location":"api-reference/","title":"API reference","text":""},{"location":"api-reference/#api-reference","title":"API reference","text":"<p>All functions accept scalars or array-likes and broadcast like NumPy ufuncs. <code>lower_tail=False</code> selects R's direct upper-tail algorithm; <code>log=True</code> works on the log scale for <code>d</code>, <code>p</code> and <code>q</code> alike.</p>"},{"location":"api-reference/#distributions","title":"Distributions","text":"accudist R notes <code>dnorm(x, mean=0.0, sd=1.0, log=False, *, out=None)</code> <code>dnorm(x, mean = 0, sd = 1, log = FALSE)</code> <code>pnorm(q, mean=0.0, sd=1.0, lower_tail=True, log=False, *, out=None)</code> <code>pnorm(q, mean = 0, sd = 1, lower.tail = TRUE, log.p = FALSE)</code> <code>qnorm(p, mean=0.0, sd=1.0, lower_tail=True, log=False, *, out=None)</code> <code>qnorm(p, mean = 0, sd = 1, lower.tail = TRUE, log.p = FALSE)</code> <code>rnorm(n, mean=0.0, sd=1.0)</code> <code>rnorm(n, mean = 0, sd = 1)</code> not R's <code>set.seed()</code> stream <code>dunif(x, min=0.0, max=1.0, log=False, *, out=None)</code> <code>dunif(x, min = 0, max = 1, log = FALSE)</code> <code>punif(q, min=0.0, max=1.0, lower_tail=True, log=False, *, out=None)</code> <code>punif(q, min = 0, max = 1, lower.tail = TRUE, log.p = FALSE)</code> <code>qunif(p, min=0.0, max=1.0, lower_tail=True, log=False, *, out=None)</code> <code>qunif(p, min = 0, max = 1, lower.tail = TRUE, log.p = FALSE)</code> <code>runif(n, min=0.0, max=1.0)</code> <code>runif(n, min = 0, max = 1)</code> not R's <code>set.seed()</code> stream <code>dgamma(x, shape, rate=None, scale=None, log=False, *, out=None)</code> <code>dgamma(x, shape, rate = 1, scale = 1/rate, log = FALSE)</code> <code>rate</code> or <code>scale</code>, not both <code>pgamma(q, shape, rate=None, scale=None, lower_tail=True, log=False, *, out=None)</code> <code>pgamma(q, shape, rate = 1, scale = 1/rate, lower.tail = TRUE, log.p = FALSE)</code> <code>rate</code> or <code>scale</code>, not both <code>qgamma(p, shape, rate=None, scale=None, lower_tail=True, log=False, *, out=None)</code> <code>qgamma(p, shape, rate = 1, scale = 1/rate, lower.tail = TRUE, log.p = FALSE)</code> <code>rate</code> or <code>scale</code>, not both <code>rgamma(n, shape, rate=None, scale=None)</code> <code>rgamma(n, shape, rate = 1, scale = 1/rate)</code> <code>rate</code> or <code>scale</code>, not both; not R's <code>set.seed()</code> stream <code>dbeta(x, shape1, shape2, ncp=None, log=False, *, out=None)</code> <code>dbeta(x, shape1, shape2, ncp, log = FALSE)</code> <code>ncp=None</code> -&gt; central algorithm; any value -&gt; non-central <code>pbeta(q, shape1, shape2, ncp=None, lower_tail=True, log=False, *, out=None)</code> <code>pbeta(q, shape1, shape2, ncp, lower.tail = TRUE, log.p = FALSE)</code> <code>ncp=None</code> -&gt; central algorithm; any value -&gt; non-central <code>qbeta(p, shape1, shape2, ncp=None, lower_tail=True, log=False, *, out=None)</code> <code>qbeta(p, shape1, shape2, ncp, lower.tail = TRUE, log.p = FALSE)</code> <code>ncp=None</code> -&gt; central algorithm; any value -&gt; non-central <code>rbeta(n, shape1, shape2, ncp=None)</code> <code>rbeta(n, shape1, shape2, ncp)</code> <code>ncp=None</code> -&gt; central algorithm; any value -&gt; non-central; not R's <code>set.seed()</code> stream <code>dlnorm(x, meanlog=0.0, sdlog=1.0, log=False, *, out=None)</code> <code>dlnorm(x, meanlog = 0, sdlog = 1, log = FALSE)</code> <code>plnorm(q, meanlog=0.0, sdlog=1.0, lower_tail=True, log=False, *, out=None)</code> <code>plnorm(q, meanlog = 0, sdlog = 1, lower.tail = TRUE, log.p = FALSE)</code> <code>qlnorm(p, meanlog=0.0, sdlog=1.0, lower_tail=True, log=False, *, out=None)</code> <code>qlnorm(p, meanlog = 0, sdlog = 1, lower.tail = TRUE, log.p = FALSE)</code> <code>rlnorm(n, meanlog=0.0, sdlog=1.0)</code> <code>rlnorm(n, meanlog = 0, sdlog = 1)</code> not R's <code>set.seed()</code> stream <code>dchisq(x, df, ncp=None, log=False, *, out=None)</code> <code>dchisq(x, df, ncp, log = FALSE)</code> <code>ncp=None</code> -&gt; central algorithm; any value -&gt; non-central <code>pchisq(q, df, ncp=None, lower_tail=True, log=False, *, out=None)</code> <code>pchisq(q, df, ncp, lower.tail = TRUE, log.p = FALSE)</code> <code>ncp=None</code> -&gt; central algorithm; any value -&gt; non-central <code>qchisq(p, df, ncp=None, lower_tail=True, log=False, *, out=None)</code> <code>qchisq(p, df, ncp, lower.tail = TRUE, log.p = FALSE)</code> <code>ncp=None</code> -&gt; central algorithm; any value -&gt; non-central <code>rchisq(n, df, ncp=None)</code> <code>rchisq(n, df, ncp)</code> <code>ncp=None</code> -&gt; central algorithm; any value -&gt; non-central; not R's <code>set.seed()</code> stream <code>df(x, df1, df2, ncp=None, log=False, *, out=None)</code> <code>df(x, df1, df2, ncp, log = FALSE)</code> <code>ncp=None</code> -&gt; central algorithm; any value -&gt; non-central <code>pf(q, df1, df2, ncp=None, lower_tail=True, log=False, *, out=None)</code> <code>pf(q, df1, df2, ncp, lower.tail = TRUE, log.p = FALSE)</code> <code>ncp=None</code> -&gt; central algorithm; any value -&gt; non-central <code>qf(p, df1, df2, ncp=None, lower_tail=True, log=False, *, out=None)</code> <code>qf(p, df1, df2, ncp, lower.tail = TRUE, log.p = FALSE)</code> <code>ncp=None</code> -&gt; central algorithm; any value -&gt; non-central <code>rf(n, df1, df2, ncp=None)</code> <code>rf(n, df1, df2, ncp)</code> <code>ncp=None</code> -&gt; central algorithm; any value -&gt; non-central; not R's <code>set.seed()</code> stream <code>dt(x, df, ncp=None, log=False, *, out=None)</code> <code>dt(x, df, ncp, log = FALSE)</code> <code>ncp=None</code> -&gt; central algorithm; any value -&gt; non-central <code>pt(q, df, ncp=None, lower_tail=True, log=False, *, out=None)</code> <code>pt(q, df, ncp, lower.tail = TRUE, log.p = FALSE)</code> <code>ncp=None</code> -&gt; central algorithm; any value -&gt; non-central <code>qt(p, df, ncp=None, lower_tail=True, log=False, *, out=None)</code> <code>qt(p, df, ncp, lower.tail = TRUE, log.p = FALSE)</code> <code>ncp=None</code> -&gt; central algorithm; any value -&gt; non-central <code>rt(n, df, ncp=None)</code> <code>rt(n, df, ncp)</code> <code>ncp=None</code> -&gt; central algorithm; any value -&gt; non-central; not R's <code>set.seed()</code> stream <code>dbinom(x, size, prob, log=False, *, out=None)</code> <code>dbinom(x, size, prob, log = FALSE)</code> <code>pbinom(q, size, prob, lower_tail=True, log=False, *, out=None)</code> <code>pbinom(q, size, prob, lower.tail = TRUE, log.p = FALSE)</code> <code>qbinom(p, size, prob, lower_tail=True, log=False, *, out=None)</code> <code>qbinom(p, size, prob, lower.tail = TRUE, log.p = FALSE)</code> <code>rbinom(n, size, prob)</code> <code>rbinom(n, size, prob)</code> not R's <code>set.seed()</code> stream <code>dcauchy(x, location=0.0, scale=1.0, log=False, *, out=None)</code> <code>dcauchy(x, location = 0, scale = 1, log = FALSE)</code> <code>pcauchy(q, location=0.0, scale=1.0, lower_tail=True, log=False, *, out=None)</code> <code>pcauchy(q, location = 0, scale = 1, lower.tail = TRUE, log.p = FALSE)</code> <code>qcauchy(p, location=0.0, scale=1.0, lower_tail=True, log=False, *, out=None)</code> <code>qcauchy(p, location = 0, scale = 1, lower.tail = TRUE, log.p = FALSE)</code> <code>rcauchy(n, location=0.0, scale=1.0)</code> <code>rcauchy(n, location = 0, scale = 1)</code> not R's <code>set.seed()</code> stream <code>dexp(x, rate=1.0, log=False, *, out=None)</code> <code>dexp(x, rate = 1, log = FALSE)</code> C takes <code>scale = 1/rate</code> <code>pexp(q, rate=1.0, lower_tail=True, log=False, *, out=None)</code> <code>pexp(q, rate = 1, lower.tail = TRUE, log.p = FALSE)</code> C takes <code>scale = 1/rate</code> <code>qexp(p, rate=1.0, lower_tail=True, log=False, *, out=None)</code> <code>qexp(p, rate = 1, lower.tail = TRUE, log.p = FALSE)</code> C takes <code>scale = 1/rate</code> <code>rexp(n, rate=1.0)</code> <code>rexp(n, rate = 1)</code> C takes <code>scale = 1/rate</code>; not R's <code>set.seed()</code> stream <code>dgeom(x, prob, log=False, *, out=None)</code> <code>dgeom(x, prob, log = FALSE)</code> <code>pgeom(q, prob, lower_tail=True, log=False, *, out=None)</code> <code>pgeom(q, prob, lower.tail = TRUE, log.p = FALSE)</code> <code>qgeom(p, prob, lower_tail=True, log=False, *, out=None)</code> <code>qgeom(p, prob, lower.tail = TRUE, log.p = FALSE)</code> <code>rgeom(n, prob)</code> <code>rgeom(n, prob)</code> not R's <code>set.seed()</code> stream <code>dhyper(x, m, n, k, log=False, *, out=None)</code> <code>dhyper(x, m, n, k, log = FALSE)</code> <code>phyper(q, m, n, k, lower_tail=True, log=False, *, out=None)</code> <code>phyper(q, m, n, k, lower.tail = TRUE, log.p = FALSE)</code> <code>qhyper(p, m, n, k, lower_tail=True, log=False, *, out=None)</code> <code>qhyper(p, m, n, k, lower.tail = TRUE, log.p = FALSE)</code> <code>rhyper(nn, m, n, k)</code> <code>rhyper(nn, m, n, k)</code> not R's <code>set.seed()</code> stream <code>dnbinom(x, size, prob=None, mu=None, log=False, *, out=None)</code> <code>dnbinom(x, size, prob, mu, log = FALSE)</code> exactly one of <code>prob</code>, <code>mu</code> <code>pnbinom(q, size, prob=None, mu=None, lower_tail=True, log=False, *, out=None)</code> <code>pnbinom(q, size, prob, mu, lower.tail = TRUE, log.p = FALSE)</code> exactly one of <code>prob</code>, <code>mu</code> <code>qnbinom(p, size, prob=None, mu=None, lower_tail=True, log=False, *, out=None)</code> <code>qnbinom(p, size, prob, mu, lower.tail = TRUE, log.p = FALSE)</code> exactly one of <code>prob</code>, <code>mu</code> <code>rnbinom(n, size, prob=None, mu=None)</code> <code>rnbinom(n, size, prob, mu)</code> exactly one of <code>prob</code>, <code>mu</code>; not R's <code>set.seed()</code> stream <code>dpois(x, lambda_, log=False, *, out=None)</code> <code>dpois(x, lambda, log = FALSE)</code> <code>ppois(q, lambda_, lower_tail=True, log=False, *, out=None)</code> <code>ppois(q, lambda, lower.tail = TRUE, log.p = FALSE)</code> <code>qpois(p, lambda_, lower_tail=True, log=False, *, out=None)</code> <code>qpois(p, lambda, lower.tail = TRUE, log.p = FALSE)</code> <code>rpois(n, lambda_)</code> <code>rpois(n, lambda)</code> not R's <code>set.seed()</code> stream <code>dweibull(x, shape, scale=1.0, log=False, *, out=None)</code> <code>dweibull(x, shape, scale = 1, log = FALSE)</code> <code>pweibull(q, shape, scale=1.0, lower_tail=True, log=False, *, out=None)</code> <code>pweibull(q, shape, scale = 1, lower.tail = TRUE, log.p = FALSE)</code> <code>qweibull(p, shape, scale=1.0, lower_tail=True, log=False, *, out=None)</code> <code>qweibull(p, shape, scale = 1, lower.tail = TRUE, log.p = FALSE)</code> <code>rweibull(n, shape, scale=1.0)</code> <code>rweibull(n, shape, scale = 1)</code> not R's <code>set.seed()</code> stream <code>dlogis(x, location=0.0, scale=1.0, log=False, *, out=None)</code> <code>dlogis(x, location = 0, scale = 1, log = FALSE)</code> <code>plogis(q, location=0.0, scale=1.0, lower_tail=True, log=False, *, out=None)</code> <code>plogis(q, location = 0, scale = 1, lower.tail = TRUE, log.p = FALSE)</code> <code>qlogis(p, location=0.0, scale=1.0, lower_tail=True, log=False, *, out=None)</code> <code>qlogis(p, location = 0, scale = 1, lower.tail = TRUE, log.p = FALSE)</code> <code>rlogis(n, location=0.0, scale=1.0)</code> <code>rlogis(n, location = 0, scale = 1)</code> not R's <code>set.seed()</code> stream <code>dwilcox(x, m, n, log=False, *, out=None)</code> <code>dwilcox(x, m, n, log = FALSE)</code> locked shared table; see <code>free_caches()</code> <code>pwilcox(q, m, n, lower_tail=True, log=False, *, out=None)</code> <code>pwilcox(q, m, n, lower.tail = TRUE, log.p = FALSE)</code> locked shared table; see <code>free_caches()</code> <code>qwilcox(p, m, n, lower_tail=True, log=False, *, out=None)</code> <code>qwilcox(p, m, n, lower.tail = TRUE, log.p = FALSE)</code> locked shared table; see <code>free_caches()</code> <code>rwilcox(nn, m, n)</code> <code>rwilcox(nn, m, n)</code> locked shared table; see <code>free_caches()</code>; not R's <code>set.seed()</code> stream <code>dsignrank(x, n, log=False, *, out=None)</code> <code>dsignrank(x, n, log = FALSE)</code> locked shared table; see <code>free_caches()</code> <code>psignrank(q, n, lower_tail=True, log=False, *, out=None)</code> <code>psignrank(q, n, lower.tail = TRUE, log.p = FALSE)</code> locked shared table; see <code>free_caches()</code> <code>qsignrank(p, n, lower_tail=True, log=False, *, out=None)</code> <code>qsignrank(p, n, lower.tail = TRUE, log.p = FALSE)</code> locked shared table; see <code>free_caches()</code> <code>rsignrank(nn, n)</code> <code>rsignrank(nn, n)</code> locked shared table; see <code>free_caches()</code>; not R's <code>set.seed()</code> stream <code>ptukey(q, nmeans, df, nranges=1.0, lower_tail=True, log=False, *, out=None)</code> <code>ptukey(q, nmeans, df, nranges = 1, lower.tail = TRUE, log.p = FALSE)</code> C argument order differs from R's; handled internally <code>qtukey(p, nmeans, df, nranges=1.0, lower_tail=True, log=False, *, out=None)</code> <code>qtukey(p, nmeans, df, nranges = 1, lower.tail = TRUE, log.p = FALSE)</code> C argument order differs from R's; handled internally"},{"location":"api-reference/#special-functions","title":"Special functions","text":"<p>These keep Rmath's C names, so <code>gammafn</code> sits unambiguously beside <code>dgamma</code>.</p> accudist R summary <code>gammafn(x)</code> <code>gamma(x)</code> <code>lgammafn(x)</code> <code>lgamma(x)</code> <code>digamma(x)</code> <code>digamma(x)</code> <code>trigamma(x)</code> <code>trigamma(x)</code> <code>tetragamma(x)</code> <code>psigamma(x, 2)</code> deprecated in R <code>pentagamma(x)</code> <code>psigamma(x, 3)</code> deprecated in R <code>psigamma(x, deriv=0.0)</code> <code>psigamma(x, deriv)</code> <code>beta(a, b)</code> <code>beta(a, b)</code> <code>lbeta(a, b)</code> <code>lbeta(a, b)</code> <code>choose(n, k)</code> <code>choose(n, k)</code> <code>lchoose(n, k)</code> <code>lchoose(n, k)</code> <code>bessel_j(x, nu)</code> <code>besselJ(x, nu)</code> <code>bessel_y(x, nu)</code> <code>besselY(x, nu)</code> <code>bessel_i(x, nu, expon_scaled=False)</code> <code>besselI(x, nu, expon.scaled)</code> <code>bessel_k(x, nu, expon_scaled=False)</code> <code>besselK(x, nu, expon.scaled)</code>"},{"location":"api-reference/#numerical-utilities","title":"Numerical utilities","text":"accudist summary <code>log1pmx(x)</code> log(1+x) - x, accurate for small x <code>log1mexp(x)</code> log(1 - exp(-x)), accurate for small and large x <code>pow1p(x, y)</code> (1 + x)^y, accurate also for <code>log1pexp(x)</code> log(1 + exp(x)) without overflow <code>lgamma1p(x)</code> lgamma(1+x), accurate for small x <code>logspace_add(logx, logy)</code> log(exp(logx) + exp(logy)) <code>logspace_sub(logx, logy)</code> log(exp(logx) - exp(logy)) <code>cospi(x)</code> cos(pi*x), exact at half-integers <code>sinpi(x)</code> sin(pi*x), exact at integers <code>tanpi(x)</code> tan(pi*x) <code>fprec(x, digits)</code> round x to <code>digits</code> significant digits <code>fround(x, digits)</code> R's round(), banker's rounding <code>fsign(x, y)</code> <code>ftrunc(x)</code> truncate toward zero <code>sign(x)</code> -1, 0, or 1"},{"location":"api-reference/#bespoke-wrappers","title":"Bespoke wrappers","text":"accudist C symbol why not a plain ufunc <code>pnorm_both</code> <code>pnorm_both</code> Writes both lower and upper tail through double* out-params; wrapper returns a 2-tuple of arrays. Cannot be a plain ufunc. <code>lgammafn_sign</code> <code>lgammafn_sign</code> Returns lgamma via return value and the sign via int* out-param; wrapper returns a 2-tuple. <code>rmultinom</code> <code>rmultinom</code> Fills an int* vector of length K; wrapper returns an (n, K) int array. Requires the RNG lock. <code>logspace_sum</code> <code>logspace_sum</code> Takes const double* + length. Expose as a reduction over the last axis, not a ufunc."},{"location":"api-reference/#rng-primitives","title":"RNG primitives","text":"accudist summary <code>unif_rand</code> Marsaglia-MultiCarry uniform on [0,1) <code>norm_rand</code> standard normal, inversion <code>exp_rand</code> standard exponential <code>set_seed</code> install global (I1, I2) state <code>get_seed</code> read global (I1, I2) state"},{"location":"api-reference/#deliberately-excluded-rmathh-symbols","title":"Deliberately excluded <code>Rmath.h</code> symbols","text":"symbol reason <code>Rlog1p</code> internal alias of log1p <code>fmax2</code> duplicate of numpy.maximum <code>fmin2</code> duplicate of numpy.minimum <code>imax2</code> trivial integer max <code>imin2</code> trivial integer min <code>R_pow</code> duplicate of numpy.power <code>R_pow_di</code> duplicate of numpy.power <code>R_isnancpp</code> internal C++ shim <code>R_finite</code> duplicate of numpy.isfinite <code>dpsifn</code> internal engine behind psigamma <code>bessel_i_ex</code> caller-supplied work array; internal <code>bessel_j_ex</code> caller-supplied work array; internal <code>bessel_k_ex</code> caller-supplied work array; internal <code>bessel_y_ex</code> caller-supplied work array; internal <code>dbinom_raw</code> internal helper; reachable via accudist.rmath only <code>dpois_raw</code> internal helper; reachable via accudist.rmath only <code>Rtanpi</code> R-internal variant of tanpi (returns +-Inf at half-integers instead of NaN); tanpi is exposed <code>rnbeta</code> declared in Rmath.h but implemented in no .c file; rbeta(..., ncp) is composed from rchisq as R does"},{"location":"compat/","title":"The scipy compatibility layer","text":"<p><code>accudist.compat</code> is a deliberately partial drop-in for <code>scipy.stats</code>. It covers the evaluation methods where tail precision matters and nothing else.</p> <pre><code>from accudist.compat import binom, poisson, norm, gamma\n\nbinom.logsf(900, 1000, 1/6)          # -1312.688   (scipy: -inf)\nbinom(1000, 1/6).logsf(900)          # frozen form\npoisson(0.1).logsf(200)              # -1331.454\ngamma.isf(1e-300, 2.5, scale=2.0)\n</code></pre>"},{"location":"compat/#supported","title":"Supported","text":"methods discrete (<code>binom</code>, <code>poisson</code>, <code>nbinom</code>, <code>geom</code>, <code>hypergeom</code>) <code>pmf</code> <code>logpmf</code> <code>cdf</code> <code>logcdf</code> <code>sf</code> <code>logsf</code> <code>ppf</code> <code>isf</code> <code>rvs</code> continuous (<code>norm</code>, <code>gamma</code>, <code>beta</code>, <code>chi2</code>, <code>t</code>, <code>f</code>, <code>expon</code>, <code>weibull_min</code>, <code>lognorm</code>, <code>cauchy</code>, <code>logistic</code>, <code>uniform</code>) <code>pdf</code> <code>logpdf</code> <code>cdf</code> <code>logcdf</code> <code>sf</code> <code>logsf</code> <code>ppf</code> <code>isf</code> <code>rvs</code> <p>Distributions take scipy's parameters, including <code>loc</code> and <code>scale</code>, and map them onto accudist internally. <code>sf</code>/<code>logsf</code>/<code>isf</code> go through R's direct upper-tail algorithms instead of <code>1 - cdf</code>; that is the whole point.</p> <p>Absent by design: <code>fit</code>, <code>expect</code>, <code>moment</code>, <code>stats</code>, <code>entropy</code>, <code>interval</code>, <code>median</code>, <code>mean</code>, <code>var</code>, <code>std</code>, <code>nnlf</code>, <code>support</code>. These are not precision problems. Accessing one raises <code>NotImplementedError</code> naming the R-flat replacement and the scipy original.</p>"},{"location":"compat/#mapping-traps-handled","title":"Mapping traps handled","text":"scipy accudist note <code>geom(p)</code> <code>dgeom(k - 1, p)</code> scipy counts trials (support from 1); R counts failures (from 0) <code>hypergeom(M, n, N)</code> <code>dhyper(k, m=n, n=M-n, k=N)</code> (population, successes, draws) vs (white, black, drawn) <code>lognorm(s, loc, scale)</code> <code>dlnorm((x-loc)/scale, 0, s)</code> scipy's <code>scale</code> is <code>exp(meanlog)</code> <code>expon(loc, scale)</code> <code>dexp((x-loc)/scale, 1)</code> <code>gamma(a, loc, scale)</code>, <code>chi2(df, loc, scale)</code>, <code>t(df, loc, scale)</code>, ... standardised <code>y = (x-loc)/scale</code>, density divided by <code>scale</code> <p><code>rvs(size=...)</code> draws from <code>accudist.default_rng()</code>; it does not accept <code>random_state</code>.</p>"},{"location":"compat/#tests","title":"Tests","text":"<ul> <li>Agreement: where scipy is accurate, every method agrees to <code>rtol=1e-12</code>   for every mapped distribution, frozen and unfrozen.</li> <li>Improvement: on the gap cases accudist is finite and correct where scipy is   not, and the test also asserts scipy's <code>-inf</code> so a scipy fix shows up.</li> <li>Absence: unimplemented methods raise with the documented message.</li> </ul>"},{"location":"development/","title":"Development and release","text":""},{"location":"development/#layout","title":"Layout","text":"path role <code>functions.toml</code> the inventory: every public function, its parameters, defaults, flags, dispatch and C symbol <code>tools/regen.py</code> generates <code>src/_ufuncs_generated.c</code>, <code>accudist/_api.py</code>, <code>accudist/rmath.py</code>, <code>docs/api-reference.md</code> from the inventory and validates every C call against <code>Rmath.h</code> <code>tools/check_inventory.py</code> every symbol in <code>Rmath.h</code> is a function, bespoke wrapper, RNG primitive or documented exclusion <code>vendor/nmath/</code> R 4.5.2 <code>src/nmath</code> + headers, pristine except for <code>vendor/patches/</code> <code>tools/sync_rmath.py</code> downloads the pinned R tarball (<code>vendor/VENDOR.toml</code>), verifies its SHA-256, extracts, applies the patches <code>src/accudist_shim.{h,c}</code> thread-local error word and message buffer, <code>calloc</code> hook <code>src/_ufuncsmodule.c</code> module init, RNG state, bespoke entry points <code>accudist/_core.py</code>, <code>_errstate.py</code>, <code>_bespoke.py</code> hand-written Python helpers <code>accudist/compat/</code> the scipy-shaped shim (imports only the public API) <code>tools/grids.py</code>, <code>tools/gen_reference.{py,R}</code> designed evaluation grids and the R reference generator <code>tests/data/*.json</code> R 4.5.2 reference values (decimal, 17 significant digits) <code>tests/reference_tolerances.py</code> the documented regions with relaxed tolerance"},{"location":"development/#workflow","title":"Workflow","text":"<pre><code>uv venv &amp;&amp; source .venv/bin/activate\nuv pip install setuptools numpy pytest hypothesis scipy mpmath\nuv pip install --no-build-isolation -e .\npython tools/regen.py --check        # generated files are up to date\npython tools/check_inventory.py      # Rmath.h fully accounted for\npytest -q                            # offline; ~330 tests, a few seconds\npytest -q -m \"not slow\"              # skip the thread stress test\n</code></pre> <p>Generated files are committed so that building from the sdist needs no tooling beyond a C compiler. CI fails if they are stale.</p>"},{"location":"development/#adding-or-changing-a-function","title":"Adding or changing a function","text":"<ol> <li>Edit <code>functions.toml</code> (public parameter order in <code>params</code>, C order in <code>c_args</code>).</li> <li><code>python tools/regen.py</code> (fails if the C arity disagrees with <code>Rmath.h</code>).</li> <li>Add a grid in <code>tools/grids.py</code> if needed and run    <code>python tools/gen_reference.py &lt;name&gt;</code> (needs R 4.5.2 on <code>PATH</code>).</li> <li><code>pytest -q -k &lt;name&gt;</code>.</li> </ol>"},{"location":"development/#bumping-r","title":"Bumping R","text":"<ol> <li>Update version, URL and SHA-256 in <code>vendor/VENDOR.toml</code>.</li> <li><code>python tools/sync_rmath.py</code>; fix any patch that no longer applies (never edit    <code>vendor/nmath/</code> in place).</li> <li><code>python tools/check_inventory.py</code> for new or removed symbols.</li> <li>Under the new R: <code>python tools/gen_reference.py</code>. Any changed reference value is    a finding; investigate before committing.</li> <li>Record it in <code>CHANGELOG.md</code>.</li> </ol>"},{"location":"development/#build-flags","title":"Build flags","text":"flag why <code>-DMATHLIB_STANDALONE -DHAVE_CONFIG_H</code> nmath's standalone mode with our <code>src/config.h</code> <code>-ffp-contract=off</code> no fused multiply-add, so results do not depend on the CPU <code>-fvisibility=hidden</code> only <code>PyInit__ufuncs</code> is exported; nmath's plain C names cannot collide with another Rmath in the process <code>-O2</code>, never <code>-ffast-math</code> nmath relies on IEEE <code>Inf</code>/<code>NaN</code> semantics"},{"location":"development/#documentation-site","title":"Documentation site","text":"<p>The site is published by hand from a checkout of <code>main</code>:</p> <pre><code>uv pip install mkdocs-material\nmkdocs build --strict        # catches broken links; CI runs this on every push\nmkdocs gh-deploy             # builds and pushes to the gh-pages branch\n</code></pre> <p>GitHub Pages must be configured to serve from the <code>gh-pages</code> branch.</p>"},{"location":"development/#release","title":"Release","text":"<p>Wheels are built by <code>.github/workflows/wheels.yml</code> with cibuildwheel for CPython 3.10 to 3.14 on manylinux x86_64/aarch64, macOS x86_64/arm64 and Windows AMD64, tested in a clean environment, and published to PyPI through trusted publishing (no API token in the repository) when a <code>v*</code> tag is pushed. The sdist is built and installed offline in the same workflow.</p> <ol> <li>Update <code>version</code> in <code>pyproject.toml</code> and <code>CHANGELOG.md</code>.</li> <li><code>git tag v0.1.0 &amp;&amp; git push --tags</code>.</li> <li>The <code>pypi</code> environment on GitHub must be configured as a trusted publisher on    PyPI for the workflow to publish.</li> </ol> <p>Versioning: <code>MAJOR.MINOR.PATCH</code>; <code>accudist.__r_version__</code> reports the vendored R. An R bump that changes any reference value is at least a minor release.</p>"},{"location":"errors/","title":"Warnings and errors","text":""},{"location":"errors/#what-nmath-does-and-what-accudist-does-instead","title":"What nmath does, and what accudist does instead","text":"<p>R's <code>nmath</code> signals problems in three ways, none of which is acceptable inside a Python extension:</p> nmath standalone behaviour accudist <code>ML_WARNING(ME_DOMAIN)</code> and friends returns <code>NaN</code>, prints nothing records a flag; the wrapper warns or raises per <code>errstate</code> <code>MATHLIB_WARNING(\"...\")</code> <code>printf</code> to stdout records the message; emitted as an <code>AccudistWarning</code> with R's own text <code>MATHLIB_ERROR(\"...\")</code> <code>printf</code>; <code>exit(1)</code> records a flag and returns <code>NaN</code>; the wrapper raises <code>MemoryError</code> <p>The C layer never prints and never exits. Both patches that achieve this are in <code>vendor/patches/</code>, and the test-suite runs every warning path in a subprocess and asserts that stdout and stderr stay empty.</p>"},{"location":"errors/#default-policy","title":"Default policy","text":"condition default class allocation failure (<code>MATHLIB_ERROR</code>) always raises <code>MemoryError</code>, not configurable domain (<code>argument out of domain</code>) warn <code>AccudistDomainWarning</code> range warn <code>AccudistRangeWarning</code> convergence failure warn <code>AccudistConvergenceWarning</code> precision loss ignore <code>AccudistPrecisionWarning</code> underflow ignore <code>AccudistUnderflowWarning</code> free-text nmath message (e.g. <code>'k' (2.40) must be integer, rounded to 2</code>) warn <code>AccudistWarning</code> <p>Precision and underflow are ignored by default because nmath raises them routinely in correct operation and R itself does not surface them.</p> <p>All warning classes derive from <code>AccudistWarning</code> (a <code>RuntimeWarning</code>); the exception classes <code>AccudistDomainError</code>, <code>AccudistRangeError</code>, <code>AccudistConvergenceError</code>, ... derive from <code>AccudistError</code>, a <code>ValueError</code>, so existing <code>except ValueError</code> handlers keep working.</p>"},{"location":"errors/#accudisterrstate","title":"<code>accudist.errstate</code>","text":"<pre><code>with ad.errstate(domain=\"raise\"):\n    ad.qbinom(0.5, -1, 0.5)          # AccudistDomainError\n\nwith ad.errstate(all=\"ignore\"):\n    ad.qbinom(0.5, -1, 0.5)          # nan, silent\n\n@ad.errstate(message=\"ignore\")       # usable as a decorator\ndef f(): ...\n\nad.get_errstate()                    # the active policy as a dict\n</code></pre> <p>Categories: <code>domain</code>, <code>range</code>, <code>noconv</code>, <code>precision</code>, <code>underflow</code>, <code>message</code>; actions: <code>'ignore'</code>, <code>'warn'</code>, <code>'raise'</code>. <code>all=</code> sets every category first. State is thread-local and nesting restores correctly.</p>"},{"location":"errors/#granularity","title":"Granularity","text":"<p>The flag word is cleared at the start of every call and read once at the end, so it reports whether any element hit a condition, not which one. That is the price of running the loop with the GIL released; per-element reporting would need Python callbacks inside the C loop.</p>"},{"location":"errors/#allocation-failures","title":"Allocation failures","text":"<p><code>wilcox</code>, <code>signrank</code>, the Bessel functions and <code>rmultinom</code> allocate. If that fails, nmath's original code would print and <code>exit(1)</code>, killing the interpreter. accudist raises <code>MemoryError</code> instead. The test-suite proves this by injecting a failure into the allocator (<code>_ufuncs._set_fail_calloc_after</code>).</p> <p><code>rmultinom</code> also uses <code>MATHLIB_ERROR</code> for a genuine argument error, a probability vector that does not sum to one; accudist reports that as a <code>ValueError</code> with R's message, not as <code>MemoryError</code>.</p>"},{"location":"errors/#caches","title":"Caches","text":"<p><code>d/p/qwilcox</code> and <code>d/p/qsignrank</code> build a distribution table in process-wide static storage. accudist guards every such call with a lock (so they serialise across threads) and frees the tables at interpreter exit. Call <code>accudist.free_caches()</code> to release the memory earlier; the tables grow with the sample sizes <code>m</code>, <code>n</code>.</p>"},{"location":"installation/","title":"Installation","text":"<p><pre><code>pip install accudist\n</code></pre> <pre><code>uv pip install accudist       # inside a uv-managed environment\nuv add accudist               # as a project dependency\n</code></pre></p> <p>accudist depends only on NumPy (1.25 or newer) at runtime.</p>"},{"location":"installation/#binary-wheels","title":"Binary wheels","text":"<p>Wheels are built with cibuildwheel for CPython 3.10, 3.11, 3.12, 3.13 and 3.14 on:</p> platform architectures Linux (manylinux, glibc) x86_64, aarch64 macOS 11+ x86_64 (Intel), arm64 (Apple silicon) Windows x86_64 (AMD64) <p>That covers the overwhelming majority of users. Not covered: PyPy, musl-based Linux (Alpine), 32-bit systems, free-threaded CPython builds, and Windows on ARM. On those, <code>pip</code> falls back to the source distribution automatically.</p>"},{"location":"installation/#building-from-source","title":"Building from source","text":"<p>The sdist is self-contained: R's <code>nmath</code> sources are committed under <code>vendor/nmath/</code>, there is no code generation at build time, and nothing is downloaded. You need</p> <ul> <li>a C compiler (gcc, clang, or MSVC 2019+; any C11 compiler),</li> <li>Python headers (<code>python3-dev</code> / <code>python3-devel</code> on Linux distributions),</li> <li>NumPy 2.x headers, which <code>pip</code> installs into the isolated build environment.</li> </ul> <p>Then any of these works:</p> <pre><code>pip install accudist --no-binary accudist                 # force a source build from PyPI\npip install git+https://github.com/hyunminkang/accudist   # latest main branch\ngit clone https://github.com/hyunminkang/accudist &amp;&amp; pip install ./accudist\nuv pip install --no-binary accudist accudist              # the same with uv\n</code></pre> <p>A source build takes about a minute. Wheels built against NumPy 2 run on NumPy 1.25+ at runtime.</p> <p>Windows</p> <p>MSVC is the supported compiler on Windows (install \"Desktop development with C++\" from the Visual Studio Build Tools). MinGW builds are untested.</p>"},{"location":"installation/#development-install","title":"Development install","text":"<pre><code>git clone https://github.com/hyunminkang/accudist &amp;&amp; cd accudist\nuv venv &amp;&amp; source .venv/bin/activate\nuv pip install setuptools numpy pytest hypothesis scipy mpmath\nuv pip install --no-build-isolation -e .\npytest -q\n</code></pre> <p><code>scipy</code> and <code>mpmath</code> are test-only dependencies; nothing under <code>accudist/</code> imports them.</p>"},{"location":"installation/#checking-the-install","title":"Checking the install","text":"<pre><code>&gt;&gt;&gt; import accudist as ad\n&gt;&gt;&gt; ad.__version__, ad.__r_version__\n('0.1.0', '4.5.2')\n&gt;&gt;&gt; ad.ppois(200, 0.1, lower_tail=False, log=True)\nnp.float64(-1331.454400621394)\n</code></pre>"},{"location":"license/","title":"Licence","text":"<p>accudist is licensed under the GNU General Public License, version 2 or (at your option) any later version.</p> <p>The reason is not a preference: accudist statically links R's <code>nmath</code> sources, which R Core distributes under GPL-2-or-later, so the extension module is a derivative work and cannot be licensed more permissively. The alternatives, a clean-room reimplementation or a runtime dependency on an installed R, would each forfeit the point of the package (R's exact algorithms, <code>pip install</code>).</p>"},{"location":"license/#what-this-means-for-you","title":"What this means for you","text":"<ul> <li>Importing accudist makes the importing work subject to the GPL. If you   distribute software that imports accudist, that software must be distributed   under GPL-compatible terms with source available. This is the same constraint   users of <code>rpy2</code> accept.</li> <li>Using accudist in your own analyses, scripts and internal tools that you do not   distribute imposes no obligation.</li> <li>Services that use accudist server-side are not \"distribution\" under GPL-2.</li> </ul>"},{"location":"license/#files","title":"Files","text":"<ul> <li><code>LICENSE</code>: GPL-2 text, as shipped with R.</li> <li><code>NOTICE</code>: copyright statements for the vendored sources.</li> <li><code>vendor/nmath/COPYING</code>: R's own copy of the licence.</li> <li><code>vendor/nmath/include/Rmath.h</code> is LGPL-2.1-or-later.</li> <li>Per-file copyright headers of The R Core Team, The R Foundation and the   individual authors are preserved verbatim under <code>vendor/nmath/</code>.</li> </ul> <p>The corresponding source is this repository and the sdist on PyPI.</p>"},{"location":"precision/","title":"Precision and the log scale","text":""},{"location":"precision/#why-direct-tails-matter","title":"Why direct tails matter","text":"<p>A survival probability of 1e-300 is perfectly representable in double precision, but <code>1 - cdf</code> cannot produce it: once the CDF rounds to 1.0 the difference is 0. On the log scale the situation is worse; <code>log(1 - cdf)</code> becomes <code>-inf</code> long before the true value is remarkable. R's <code>nmath</code> therefore implements every <code>p*</code> function with a <code>lower_tail</code> switch that changes the algorithm, and every <code>p*</code>/<code>q*</code> with a <code>log_p</code> switch, so that</p> <pre><code>ad.ppois(200, 0.1, lower_tail=False, log=True)   # -1331.4544006213939\n</code></pre> <p>is computed as a log-sum rather than as <code>log(1 - 0.99999...)</code>. accudist exposes those switches verbatim; it adds no numerics of its own.</p>"},{"location":"precision/#the-correctness-contract","title":"The correctness contract","text":"<p>For every function on the grids in <code>tests/data/</code>, accudist agrees with R 4.5.2 to eight significant digits (relative tolerance 1e-8). NaN matches any NaN, infinities match exactly, and values below 1e-300 compare with an absolute floor.</p> <p>This is deliberately not bit-exactness. accudist compiles the very same C source R compiles, but with a different compiler, on a different libm, sometimes with a different <code>long double</code>. Those produce last-bit differences that are irrelevant to any statistical use and would make the test-suite platform dependent. Eight digits is the promise that can be kept everywhere.</p> <p>The grids (about 21 000 points) are designed rather than sampled: every <code>p</code>/<code>q</code> function under all four <code>lower_tail</code> x <code>log</code> combinations; far tails (<code>1e-300</code>, <code>1e-100</code>, <code>1e-15</code>, <code>1 - 1e-10</code>, log-probabilities down to <code>-1e5</code>); support boundaries, <code>0</code>, <code>+-inf</code>, <code>NaN</code>; domain errors; tiny and huge parameters; <code>ncp=None</code> vs <code>ncp=0</code>; both <code>prob</code> and <code>mu</code>; both <code>rate</code> and <code>scale</code>.</p>"},{"location":"precision/#where-eight-digits-is-impossible-and-why","title":"Where eight digits is impossible, and why","text":"<p>A few regions of R's own algorithms are not stable to eight digits. They are listed, with the reason, in <code>tests/reference_tolerances.py</code>, and the relaxed tolerance applies only there:</p> region relaxed to why <code>qt</code>, <code>qchisq</code>, <code>qbeta</code>, <code>qf</code> with <code>ncp</code> and <code>1 - p &lt; 1e-8</code> 1e-6 the quantile inverts a <code>p*</code> function accurate to ~1e-12; the tail mass being matched has only 4-6 digits <code>dt</code> with <code>ncp</code> and <code>|x| &gt;= 50</code> 5e-2 <code>dnt</code> is a difference of two <code>pnt</code> values; in the far tail they cancel catastrophically and R's answer itself has two digits <code>ptukey</code> 1e-6, or 1e-13 absolute numerical quadrature; the upper tail is <code>1 - lower</code> <code>qtukey</code> 1e-5 the secant iteration stops at <code>|x1 - x0| &lt; 1e-4</code>, as R documents <p>Outside these regions the suite has zero tolerance for a ninth-digit surprise: a mismatch is a bug in the wrapper until proven otherwise.</p>"},{"location":"precision/#platform-notes","title":"Platform notes","text":"<ul> <li>Builds use <code>-ffp-contract=off</code> (no fused multiply-add) so that results do not   depend on whether the CPU has FMA. R's own binaries differ here: R for Apple   silicon is built with contraction on, which is why a bit-exact comparison with   R on a Mac fails in a few cancellation-prone spots even though the C source is   identical.</li> <li>nmath's <code>LDOUBLE</code> accumulators are compiled as plain <code>double</code> on every   platform (R's own <code>--disable-long-double</code> configuration, and what R already   does on Apple silicon and Windows). With the platform <code>long double</code> instead,   the non-central beta/chi-squared/t/F tails differ between x86_64 (80-bit),   Linux/aarch64 (128-bit) and the double platforms by far more than the last   few bits; R itself disagrees with R across those platforms there. Plain   double makes every accudist wheel return identical numbers, and they match   the reference values, which were generated with R on Apple silicon.</li> <li>R's <code>cospi</code>/<code>sinpi</code>/<code>tanpi</code> on macOS call Apple's <code>__cospi</code> family; accudist   uses nmath's portable implementation everywhere. They agree except for   arguments around 1e15 where Apple's version is wrong.</li> </ul>"},{"location":"precision/#utilities-checked-against-mpmath","title":"Utilities checked against mpmath","text":"<p><code>log1pmx</code>, <code>log1pexp</code>, <code>log1mexp</code>, <code>lgamma1p</code>, <code>logspace_add</code>, <code>logspace_sub</code> have no R-level twin (R exports them only through <code>Rmath.h</code>), so they are checked against 50-digit <code>mpmath</code> references to 1e-13 relative.</p>"},{"location":"quickstart/","title":"Quickstart","text":"<pre><code>import numpy as np\nimport accudist as ad\n</code></pre>"},{"location":"quickstart/#the-four-letters","title":"The four letters","text":"<p>Every distribution has up to four functions, exactly as in R:</p> prefix meaning first argument <code>d</code> density / mass <code>x</code> <code>p</code> distribution function (CDF) <code>q</code> <code>q</code> quantile function <code>p</code> <code>r</code> random draws <code>n</code> (the number of draws) <pre><code>ad.dnorm(1.96)                 # 0.0584...\nad.pnorm(1.96)                 # 0.9750...\nad.qnorm(0.975)                # 1.9599...\nad.rnorm(3)                    # array([...])\n</code></pre>"},{"location":"quickstart/#tails-and-the-log-scale","title":"Tails and the log scale","text":"<p><code>lower_tail=False</code> selects R's direct upper-tail algorithm; it is not <code>1 - cdf</code>. <code>log=True</code> returns natural logarithms for <code>d</code>, <code>p</code> and <code>q</code> alike (R spells it <code>log.p=</code> for <code>p</code>/<code>q</code>; accudist uses <code>log=</code> everywhere).</p> <pre><code>ad.ppois(200, 0.1, lower_tail=False)            # 0.0     underflows in linear space, like R\nad.ppois(200, 0.1, lower_tail=False, log=True)  # -1331.45...  the actual answer\nad.qnorm(-1000, log=True)                        # -44.6157...  quantile of a log-probability\nad.pbinom(900, 1000, 1/6, lower_tail=False, log=True)   # -1312.69\n</code></pre>"},{"location":"quickstart/#arrays-and-broadcasting","title":"Arrays and broadcasting","text":"<p>Everything is a NumPy ufunc underneath: array arguments broadcast, <code>out=</code> writes into a preallocated array, and scalar inputs return <code>numpy.float64</code>.</p> <pre><code>q = np.arange(100, 105)\nad.ppois(q, 0.1, lower_tail=False, log=True)          # shape (5,)\nad.dnorm(np.zeros((3, 1)), mean=[0.0, 1.0])           # shape (3, 2)\n\nout = np.empty(5)\nad.ppois(q, 0.1, lower_tail=False, log=True, out=out)\n</code></pre>"},{"location":"quickstart/#parameterisations-the-r-way","title":"Parameterisations, the R way","text":"<pre><code>ad.pgamma(2.0, shape=3, rate=2.0)      # R: pgamma(2, 3, rate = 2)\nad.pgamma(2.0, shape=3, scale=0.5)     # same value\nad.pnbinom(3, size=5, mu=7.0)          # R's mu parameterisation\nad.pchisq(3.0, df=5, ncp=1.5)          # non-central chi-squared\nad.pchisq(3.0, df=5)                   # central algorithm (ncp=None), as in R when ncp is missing\nad.dpois(2, lambda_=3.0)               # lambda is a Python keyword\n</code></pre>"},{"location":"quickstart/#special-functions","title":"Special functions","text":"<p>Special functions keep Rmath's C names so <code>gammafn</code> sits unambiguously beside <code>dgamma</code>:</p> <pre><code>ad.gammafn(5.0)            # R: gamma(5)     -&gt; 24\nad.lgammafn(100.0)         # R: lgamma(100)\nad.psigamma(2.0, deriv=1)  # R: psigamma(2, 1) == trigamma(2)\nad.bessel_i(1.0, 0.5, expon_scaled=True)   # R: besselI(1, 0.5, TRUE)\nad.lchoose(1000, 500)      # R: lchoose(1000, 500)\n</code></pre>"},{"location":"quickstart/#warnings","title":"Warnings","text":"<p>Invalid arguments give <code>NaN</code> and a warning, like R's \"NaNs produced\":</p> <pre><code>ad.qbinom(0.5, -1, 0.5)          # nan, AccudistDomainWarning\nwith ad.errstate(domain=\"raise\"):\n    ad.qbinom(0.5, -1, 0.5)      # raises AccudistDomainError (a ValueError)\nwith ad.errstate(all=\"ignore\"):\n    ad.qbinom(0.5, -1, 0.5)      # nan, silently\n</code></pre>"},{"location":"quickstart/#random-draws","title":"Random draws","text":"<pre><code>ad.set_r_seed(42)                 # like R's set.seed(42) under RNGkind(\"Marsaglia-Multicarry\")\nad.rpois(5, 3.0)\nrng = ad.RNG.from_r_seed(7)       # an independent, thread-safe stream\nrng.rgamma(1000, shape=2.5, rate=2.0)\n</code></pre> <p>See Random numbers for what is and is not reproducible against R.</p>"},{"location":"r-mapping/","title":"R to accudist","text":""},{"location":"r-mapping/#naming-rules","title":"Naming rules","text":"<ol> <li>Distribution functions use R's user-level names: <code>ppois</code>, <code>dbinom</code>, <code>qnorm</code>,    <code>rgamma</code>, <code>ptukey</code>, <code>pwilcox</code>, <code>psignrank</code>.</li> <li>Special functions use Rmath's C names: <code>gammafn</code>, <code>lgammafn</code>, <code>digamma</code>,    <code>trigamma</code>, <code>psigamma</code>, <code>beta</code>, <code>lbeta</code>, <code>choose</code>, <code>lchoose</code>, <code>bessel_i/j/k/y</code>.    R calls these <code>gamma</code>, <code>lgamma</code>, <code>besselI</code>, ...; the C names avoid shadowing    <code>math.gamma</code> and sitting confusingly next to <code>dgamma</code>.</li> <li>Parameter names mirror R exactly, with one exception forced by Python:    <code>lambda</code> becomes <code>lambda_</code>.</li> <li><code>log=</code> is the only spelling of the log-scale flag, for <code>d</code>, <code>p</code>, <code>q</code> alike.    R uses <code>log=</code> for densities and <code>log.p=</code> for <code>p</code>/<code>q</code>. <code>log_p=</code> raises <code>TypeError</code>.</li> <li><code>out=</code> is keyword-only; <code>lower_tail</code> and <code>log</code> may be positional, as in R.</li> </ol>"},{"location":"r-mapping/#signature-shape","title":"Signature shape","text":"<pre><code>d&lt;dist&gt;(x, &lt;params...&gt;, log=False, *, out=None)\np&lt;dist&gt;(q, &lt;params...&gt;, lower_tail=True, log=False, *, out=None)\nq&lt;dist&gt;(p, &lt;params...&gt;, lower_tail=True, log=False, *, out=None)\nr&lt;dist&gt;(n, &lt;params...&gt;)\n</code></pre> <p>Quantiles of discrete distributions are returned as doubles (<code>ad.qpois(0.5, 4.0)</code> is <code>4.0</code>), and <code>r*</code> return <code>float64</code> arrays, exactly as R does.</p>"},{"location":"r-mapping/#dispatch","title":"Dispatch","text":""},{"location":"r-mapping/#ncpnone-selects-the-central-algorithm","title":"<code>ncp=None</code> selects the central algorithm","text":"<p>R decides between the central and non-central code paths by <code>missing(ncp)</code>, not by its value: <code>pchisq(q, df, ncp = 0)</code> calls <code>pnchisq</code>. accudist reproduces this with <code>None</code> as the sentinel.</p> <pre><code>ad.pchisq(3, df=5)            # C pchisq   (central)\nad.pchisq(3, df=5, ncp=0.0)   # C pnchisq  (non-central with ncp 0; may differ in the last digits)\nad.pchisq(3, df=5, ncp=1.5)   # C pnchisq\n</code></pre> <p>Applies to <code>beta</code>, <code>chisq</code>, <code>f</code>, <code>t</code> for <code>d</code>, <code>p</code>, <code>q</code>, <code>r</code>. Only <code>rchisq</code> has a C non-central sampler; <code>rbeta</code>, <code>rf</code>, <code>rt</code> with <code>ncp</code> are composed from <code>rchisq</code> and <code>rnorm</code> in the same order R composes them.</p>"},{"location":"r-mapping/#prob-xor-mu-negative-binomial","title":"<code>prob</code> xor <code>mu</code> (negative binomial)","text":"<pre><code>ad.pnbinom(q, size, prob=0.3)   # C pnbinom\nad.pnbinom(q, size, mu=7.0)     # C pnbinom_mu\nad.pnbinom(q, size, 0.3, 7.0)   # TypeError: 'prob' and 'mu' both specified\nad.pnbinom(q, size)             # TypeError: requires one of 'prob' or 'mu'\n</code></pre>"},{"location":"r-mapping/#rate-xor-scale-gamma","title":"<code>rate</code> xor <code>scale</code> (gamma)","text":"<p>R warns if both are given but consistent (<code>rate * scale</code> within 1e-15 of 1) and errors otherwise; accudist does the same (<code>AccudistWarning</code> / <code>TypeError</code>).</p> <pre><code>ad.pgamma(q, shape, rate=2.0)              # scale = 0.5\nad.pgamma(q, shape, scale=0.5)             # same\nad.pgamma(q, shape, rate=2.0, scale=0.5)   # warning\nad.pgamma(q, shape, rate=2.0, scale=9.0)   # TypeError\n</code></pre>"},{"location":"r-mapping/#exponential-rate","title":"Exponential: <code>rate</code>","text":"<p>The C library takes a scale; accudist passes <code>1 / rate</code> exactly as R's wrapper does.</p>"},{"location":"r-mapping/#argument-order-hazards-handled-for-you","title":"Argument-order hazards handled for you","text":"<ul> <li><code>ptukey</code> / <code>qtukey</code>: R presents <code>(q, nmeans, df, nranges)</code> but the C symbol   takes <code>(q, nranges, nmeans, df)</code>. accudist keeps R's order.</li> <li><code>hyper</code>: <code>phyper(q, m, n, k)</code> where <code>n</code> is the number of black balls, so the   draw count of <code>rhyper</code> is called <code>nn</code>: <code>rhyper(nn, m, n, k)</code>. Same for   <code>rwilcox(nn, m, n)</code> and <code>rsignrank(nn, n)</code>.</li> <li><code>bessel_i</code> / <code>bessel_k</code>: <code>expon_scaled=True</code> maps to the C <code>expo = 2</code>.</li> </ul>"},{"location":"r-mapping/#translation-table","title":"Translation table","text":"R accudist <code>ppois(q, lambda, lower.tail=FALSE, log.p=TRUE)</code> <code>ad.ppois(q, lambda_, lower_tail=False, log=True)</code> <code>dbinom(x, size, prob, log=TRUE)</code> <code>ad.dbinom(x, size, prob, log=True)</code> <code>qt(p, df, ncp)</code> <code>ad.qt(p, df, ncp=ncp)</code> <code>pgamma(q, shape, rate=2)</code> <code>ad.pgamma(q, shape, rate=2.0)</code> <code>dexp(x, rate)</code> <code>ad.dexp(x, rate)</code> <code>ptukey(q, nmeans, df, nranges)</code> <code>ad.ptukey(q, nmeans, df, nranges)</code> <code>pwilcox(q, m, n)</code> <code>ad.pwilcox(q, m, n)</code> <code>gamma(x)</code> / <code>lgamma(x)</code> <code>ad.gammafn(x)</code> / <code>ad.lgammafn(x)</code> <code>besselI(x, nu, expon.scaled=TRUE)</code> <code>ad.bessel_i(x, nu, expon_scaled=True)</code> <code>psigamma(x, deriv=2)</code> <code>ad.psigamma(x, deriv=2)</code> <code>signif(x, d)</code> / <code>round(x, d)</code> <code>ad.fprec(x, d)</code> / <code>ad.fround(x, d)</code> <code>set.seed(s)</code> under <code>RNGkind(\"Marsaglia-Multicarry\")</code> <code>ad.set_r_seed(s)</code>"},{"location":"r-mapping/#escape-hatches","title":"Escape hatches","text":"namespace contract <code>accudist</code> the supported API: defaults, dispatch, <code>errstate</code>, docstrings <code>accudist.rmath</code> 1:1 with <code>Rmath.h</code>: C argument order, <code>0/1</code> flags, no dispatch, no error translation; where <code>pnchisq</code>, <code>dbinom_raw</code>, <code>dpois_raw</code> live <code>accudist._ufuncs</code> the raw ufuncs themselves; private, fastest <pre><code>from accudist import rmath\nrmath.pnchisq(3.0, 5.0, 1.5, 1, 0)      # (x, df, ncp, lower_tail, log_p)\n</code></pre>"},{"location":"rng/","title":"Random numbers","text":""},{"location":"rng/#the-headline-caveat","title":"The headline caveat","text":"<p>accudist's <code>r*</code> functions do not reproduce R's default <code>set.seed()</code> streams.</p> <p>R's default uniform generator is Mersenne-Twister. Standalone Rmath, which accudist vendors, uses a Marsaglia-MultiCarry generator with two 32-bit state words (<code>sunif.c</code>). The sampling algorithms (<code>rpois</code>, <code>rbinom</code>, <code>rgamma</code>, ...) are R's; only the uniform stream underneath differs, which is enough to make every draw different.</p>"},{"location":"rng/#what-is-reproducible-against-r","title":"What is reproducible against R","text":"<p>R can be switched to the same generator, and then accudist matches it exactly:</p> <p><pre><code>RNGkind(\"Marsaglia-Multicarry\", normal.kind = \"Inversion\", sample.kind = \"Rejection\")\nset.seed(42)\nrunif(3); rnorm(3); rpois(3, 4); rgamma(3, 2)\n</code></pre> <pre><code>import accudist as ad\nad.set_r_seed(42)\nad.runif(3); ad.rnorm(3); ad.rpois(3, 4.0); ad.rgamma(3, 2.0)   # identical to R above\n</code></pre></p> <p><code>set_r_seed</code> applies R's own seed scrambling (<code>RNG_Init</code> in <code>src/main/RNG.c</code>) to produce the two state words. The test-suite pins these values against R 4.5.2 for <code>runif</code>, <code>rnorm</code>, <code>rpois</code>, <code>rbinom</code>, <code>rgamma</code>, <code>rexp</code>, <code>rbeta</code>, <code>rt</code>, the non-central <code>rchisq</code>, <code>rhyper</code>, <code>rwilcox</code> and <code>rsignrank</code>.</p> <p>Note</p> <p>R warns that Marsaglia-Multicarry \"has poor statistical properties\". It is a 1990s generator with a period around 2^60. For serious simulation work use NumPy's <code>Generator</code> for the uniforms; accudist's value is in the <code>d</code>/<code>p</code>/<code>q</code> functions. The <code>r*</code> functions exist for completeness and for reproducing R.</p>"},{"location":"rng/#streams","title":"Streams","text":"<p><code>accudist.RNG</code> holds one <code>(i1, i2)</code> state. Every draw installs that state into the C library under a process-wide lock, runs the whole vectorised draw, and saves the state back, so streams are independent and thread-safe.</p> <pre><code>rng = ad.RNG.from_r_seed(7)         # scrambled like R's set.seed(7)\nrng = ad.RNG(123456789, 987654321)  # raw state words (avoid small values, see below)\nrng.rnorm(5); rng.rpois(5, 2.0); rng.rmultinom(3, size=10, prob=[0.2, 0.3, 0.5])\nrng.get_seed(); rng.set_seed(i1, i2); rng.set_r_seed(7)\n</code></pre> <p>The module-level <code>ad.rnorm(...)</code>, <code>ad.set_seed</code>, <code>ad.set_r_seed</code>, <code>ad.get_seed</code> operate on <code>ad.default_rng()</code>, which starts in Rmath's default state <code>(1234, 5678)</code>.</p> <p>Small raw seeds are correlated</p> <p>Marsaglia-MultiCarry mixes small raw state words badly: <code>RNG(1, 2)</code> and <code>RNG(3, 4)</code> produce visibly correlated normals. R avoids this by scrambling the user's seed; <code>RNG.from_r_seed</code> / <code>set_r_seed</code> do the same. Prefer them.</p>"},{"location":"rng/#signatures","title":"Signatures","text":"<pre><code>r&lt;dist&gt;(n, &lt;params...&gt;)     # -&gt; float64 array of length n\n</code></pre> <p><code>n</code> is the draw count. Other parameters recycle to length <code>n</code> as in R (<code>rnorm(4, mean=[0, 10])</code> alternates); a parameter longer than <code>n</code> is an error. Where R names the count <code>nn</code> because <code>n</code> is a distribution parameter, so does accudist: <code>rhyper(nn, m, n, k)</code>, <code>rwilcox(nn, m, n)</code>, <code>rsignrank(nn, n)</code>. Discrete draws are returned as <code>float64</code>, as R does.</p> <p>Non-central draws follow R's compositions, consuming the underlying streams in the same order:</p> function with <code>ncp</code> <code>rchisq(n, df, ncp)</code> C <code>rnchisq</code> <code>rbeta(n, s1, s2, ncp)</code> <code>X = rchisq(n, 2*s1, ncp); X / (X + rchisq(n, 2*s2))</code> <code>rf(n, df1, df2, ncp)</code> <code>(rchisq(n, df1, ncp)/df1) / (rchisq(n, df2)/df2)</code> <code>rt(n, df, ncp)</code> <code>rnorm(n, ncp) / sqrt(rchisq(n, df)/df)</code>"},{"location":"rng/#primitives","title":"Primitives","text":"<p><code>unif_rand(n)</code>, <code>norm_rand(n)</code>, <code>exp_rand(n)</code> expose Rmath's generator directly; <code>rmultinom(n, size, prob)</code> returns an <code>(n, K)</code> integer array (R returns the transpose).</p>"}]}