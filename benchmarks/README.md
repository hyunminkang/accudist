# Benchmarks

The maintained methodology, recorded results, and interpretation live in the
[benchmark documentation](https://hyunminkang.github.io/accudist/benchmarks/).

Run `python benchmarks/benchmark.py` from an installed checkout to reproduce the
comparison.
